# 筑梦启动器 1.3.1

修复核心更新提示“av 的旧版本不是可逆的精确版本”。整合包的 av 17.1.0 已满足新版 `av>=17.0.0`，更新器现在通过依赖解析判断是否需要安装，并从实际环境记录回滚版本。

新增启动器在线更新。首次从 1.3.0 升级，请下载 `zhumeng-first-update-1.3.1.zip`，关闭启动器并解压到原整合包根目录，再双击 `Install-Launcher-Update.cmd`。之后使用维护中心内的“检查启动器更新”。

`zhumeng-launcher-1.3.1.zip` 和 `launcher-update.json` 供在线更新器读取；普通用户优先下载首次升级包。

已实际验证核心 0.33.1 → 0.35.0、失败后核心和依赖回滚、av 安装/恢复、中文带空格路径、补丁损坏拦截和文件占用恢复。Windows 11 / RTX 5070 Ti 本机 GPU 启动通过；Windows 10、RTX 30/40 等其他机器尚未完成本轮实机验证。详情见仓库验证报告。

无需重传整合包。补丁不包含 Python、模型或第三方节点；新核心的所有第三方工作流兼容性不属于本次通过结论。cu128 运行 0.35.0 会提示部分 cu130 优化能力不可用，本补丁不会强制更换显卡运行时。
