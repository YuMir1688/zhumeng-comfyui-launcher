# 1.3.4 实测记录

日期：2026-09-15。Windows 11，RTX 5070 Ti，驱动 595.97，Python 3.11.6，torch 2.11.0+cu128。

## 已确认并修复

1. Get-FileHash 在后台不可用：核心更新器改为 .NET SHA-256，禁用模块自动加载的后台已知哈希测试通过。
2. 后台 Python 卡住：Windows PowerShell 5.1 Start-Job 内不重定向输入时，pip --version 超时；重定向输入并关闭后通过。核心与扩展维护进程均修复。
3. 网络错误导致 WPF 退出：保留 1.3.3 修复，13 种错误输入、6 种 WPF 回调回归通过。

## 真实操作结果

| 项目 | 实际结果 |
|---|---|
| 当前包核心更新 | 同样使用后台任务执行，0.33.1 → 0.35.0，依赖安装及隔离健康检查完成；17:09:11 日志为 complete |
| 全插件启动 | 未禁用自定义节点，127.0.0.1:1081 服务启动，system_stats 返回 0.35.0，object_info 返回 5173 个节点定义 |
| 无模型任务 | EmptyImage → ImageScale → SaveImage，任务 483bc420-65e5-4759-9a0d-bc514462f823 状态 success，输出 256×256 PNG |
| Comfy-MCP 安装 | 官方 GitHub 归档安装成功，新增 Python 依赖 0；这不代表额外 MCP 客户端联调完成 |
| 扩展管理 | 该测试扩展 Disable、Enable、Remove 均返回成功；测试安装已移除并保留恢复备份 |
| 补丁安全测试 | 安装、中文空格路径、占用文件回滚、错误哈希拒绝通过 |
| Windows CI | 34951344757 成功，覆盖网络错误、后台 Python、哈希、自检和补丁回滚 |
| 文件同步 | 四个本地目录的补丁文件逐项 SHA-256 与最终 1.3.4 发布补丁一致 |

测试启动的 1081 服务已结束，不影响原有其他 ComfyUI 服务。测试图片和运行状态不进入新建干净分发目录。

## 不可夸大的范围

- 用户明确本轮不处理模型；测试任务不涉及扩散模型推理、视频生成或配音模型。
- 全插件能加载和 API 能响应，不等于每个第三方插件的每个功能通过。日志仍有 protobuf MessageFactory 兼容提示及 cu130 优化提示。
- 原有已记录的 21 项 Python 依赖元数据冲突未在此补丁中全部修复。不得以已知冲突清单代替功能验收。
- 首次完整启动测试直接调用 Python，没有加载启动器的 SoX PATH 配置，因此该日志中的 SoX 提示不能直接判断包缺少 SoX；包带有 tools/sox。
- Windows 10、其他 RTX 30/40/50 实机并未在本轮验证。

新分发目录：F:\ComfyUI一键包\ComfyUI_发布版_1.3.4。原目录保留，不能将旧目录名当成当前启动器版本。
