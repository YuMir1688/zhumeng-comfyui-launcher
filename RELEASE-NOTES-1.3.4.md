# 筑梦启动器 1.3.4

包含 1.3.3 的网络异常闪退修复，并修复核心更新后台找不到 Get-FileHash 时更新失败的问题。核心更新器使用 .NET SHA-256，不依赖该命令的模块自动加载，不跳过校验。

旧版无法完成在线升级时，将 zhumeng-first-update-1.3.4.zip 解压到整合包根目录，关闭启动器后运行 Install-Launcher-Update.cmd。在线更新器会读取本版本的 launcher-update.json。

此补丁仅更新启动器及维护脚本，不更换核心、Python、CUDA、模型或节点。后台 SHA-256 已用已知结果验证，网络异常回归及补丁安装/回滚测试通过；不代表所有第三方插件或硬件组合已验收。分发前仍需完成整包清理和验收。

另修复 PowerShell 后台任务的标准输入继承导致 Python 卡住的问题。核心更新和插件维护进程均使用独立且立即关闭的标准输入。在用户当前包上，核心 0.33.1 → 0.35.0 已实际完成依赖安装及隔离健康检查；这不等于所有插件工作流均已通过。
